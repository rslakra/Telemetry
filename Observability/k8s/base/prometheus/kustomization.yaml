apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - configmap.yaml
  - service.yaml
  - statefulset.yaml
  - servicemonitor.yaml
  - pdb.yaml
  - hpa.yaml

commonLabels:
  app: prometheus
  component: monitoring

