apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  namespace: observability
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: metrics
    port: 9100
    targetPort: 9100
    protocol: TCP
  selector:
    app: node-exporter

